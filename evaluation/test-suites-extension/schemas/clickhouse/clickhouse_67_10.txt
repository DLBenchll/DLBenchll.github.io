SELECT arrayElement([1, 2], 2), indexOf(['a', 'b', 'c'], NULL);
SELECT indexOf(['a', NULL], 'b'), indexOf([0, NULL], 1) + 0, indexOf([0.0, NULL], 1.0) + 0.0, indexOf([0.0e1, NULL], 1.0e1) + 0.0e1;
SELECT indexOf(['a', NULL], NULL), indexOf([0, NULL], NULL) + 0, indexOf([0.0, NULL], NULL) + 0.0, indexOf([0.0e1, NULL], NULL) + 0.0e1;
DROP TABLE IF EXISTS t1;
DROP TABLE IF EXISTS t2;
CREATE TABLE t1 (id Int32 NOT NULL )ENGINE=MergeTree ORDER BY id;
CREATE TABLE t2 (id Int32 NOT NULL , val Int32 NOT NULL)engine=MergeTree ORDER BY id;
INSERT INTO t1 VALUES (1), (2), (4);
INSERT INTO t2 VALUES (1, 1), (2, 1), (3, 1), (4, 2);
SELECT one.id, arrayElement(['one', 'two'], two.val) FROM t1 AS one, t2 AS two WHERE two.id = one.id;
